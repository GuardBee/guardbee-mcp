import { readFileSync, existsSync } from "fs";
import { resolve, join } from "path";
const DEFAULTS = {
    failOn: "any",
    maxFiles: 5000,
    exclude: [],
    allowlist: [],
};
function findConfigFile(startDir) {
    const names = ["guardbee.yml", "guardbee.yaml", ".guardbee.yml"];
    let dir = resolve(startDir);
    for (let i = 0; i < 5; i++) {
        for (const name of names) {
            const p = join(dir, name);
            if (existsSync(p))
                return p;
        }
        const parent = resolve(dir, "..");
        if (parent === dir)
            break;
        dir = parent;
    }
    return null;
}
// Minimal YAML parser — only handles the flat key: value and list items we need
function parseYaml(content) {
    const root = {};
    let currentSection = null;
    let currentKey = null;
    let currentList = null;
    for (const rawLine of content.split("\n")) {
        const line = rawLine.replace(/#.*$/, ""); // strip comments
        if (!line.trim())
            continue;
        const indent = line.match(/^(\s*)/)?.[1].length ?? 0;
        // Top-level section (no indent, ends with colon)
        if (indent === 0 && line.trim().endsWith(":")) {
            if (currentKey && currentList) {
                root[currentSection][currentKey] = currentList;
                currentList = null;
                currentKey = null;
            }
            currentSection = line.trim().slice(0, -1);
            root[currentSection] = {};
            continue;
        }
        // Nested key: value (indent 2)
        if (indent === 2 && currentSection) {
            if (currentKey && currentList) {
                root[currentSection][currentKey] = currentList;
                currentList = null;
                currentKey = null;
            }
            const colonIdx = line.indexOf(":");
            if (colonIdx === -1)
                continue;
            const key = line.slice(0, colonIdx).trim();
            const value = line.slice(colonIdx + 1).trim();
            if (value === "") {
                // Start of a list
                currentKey = key;
                currentList = [];
            }
            else {
                root[currentSection][key] = value;
            }
            continue;
        }
        // List item (indent 4, starts with -)
        if (indent === 4 && currentList !== null) {
            const item = line.trim().replace(/^-\s*/, "").replace(/^['"]|['"]$/g, "");
            currentList.push(item);
            continue;
        }
    }
    // Flush last list
    if (currentKey && currentList && currentSection) {
        root[currentSection][currentKey] = currentList;
    }
    return root;
}
export function loadConfig(searchDir, cliOverrides = {}) {
    const configPath = findConfigFile(searchDir);
    if (!configPath)
        return { ...DEFAULTS, ...cliOverrides };
    let fileConfig = {};
    try {
        const raw = readFileSync(configPath, "utf8");
        const parsed = parseYaml(raw);
        const section = parsed["secret-scanner"];
        if (section) {
            if (typeof section["fail-on"] === "string")
                fileConfig.failOn = section["fail-on"];
            if (typeof section["max-files"] === "string")
                fileConfig.maxFiles = parseInt(section["max-files"], 10);
            if (Array.isArray(section["exclude"]))
                fileConfig.exclude = section["exclude"];
            if (Array.isArray(section["allowlist"]))
                fileConfig.allowlist = section["allowlist"];
        }
    }
    catch {
        // ignore parse errors, use defaults
    }
    return { ...DEFAULTS, ...fileConfig, ...cliOverrides };
}
export function configFilePath(searchDir) {
    return findConfigFile(searchDir);
}
//# sourceMappingURL=config.js.map