startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    config => ({
      command: 'npx',
      args: ['-y', '@guardbee/mcp-dns-intelligence@latest']
    })
